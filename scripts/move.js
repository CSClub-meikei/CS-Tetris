// Generated by CoffeeScript 1.10.0
var hey, move;

move = function(direction, minus) {
  var fun;
  if (direction == null) {
    direction = "top";
  }
  if (minus == null) {
    minus = 1;
  }
  return $('.falling_red').each(fun = function() {
    var blockCSS;
    blockCSS = parseInt($(this).css("" + direction).slice(0, -2));
    if (minus === 1) {
      if (("" + direction) === "top") {
        if (blockCSS + 60 < 600) {
          blockCSS += 30;
        }
      } else if (("" + direction) === "left") {
        if (blockCSS + 90 < 600) {
          blockCSS += 30;
        }
      }
    } else {
      if (blockCSS > 0) {
        blockCSS -= 30;
      }
    }
    console.log(blockCSS);
    return $(this).css("" + direction, blockCSS);
  });
};

$(window).on("keydown", function(e) {
  switch (e.keyCode) {
    case 40:
      return move();
    case 39:
      return move("left", 1);
    case 37:
      return move("left", -1);
  }
});

$(document).on("ready", function() {
  return setInterval("move()", 500);
});

hey = function() {
  return window.alert("Hello");
};
